# ncf-recommendation-engine
A production-ready Neural Collaborative Filtering (NCF) recommendation engine implementing NeuMF (GMF + MLP) with PyTorch, optimized using Optuna HPO and INT8 quantization.
